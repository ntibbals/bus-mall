'use-strict';

var products = [];
function Product(name, src) {
    this.name = name,
    this.src = src,
    this.votes = votes,
}

var tracker = { // bulk of code written into properties and methods within tracker , get element by id (main-content); 
  Product();
  totalClicks: 0, // counter for total clicks

  mainEl = document.getElementById('bus') // should be appending some content var username in four funcions is okay because it's locally scoped

  getRandomIndex: function () { //generate random number within an array // can 

  },
  getUniqueImages: function () { //can do one at a time or all at once

  }
  renderImages: function () { // all ideas or scaffold - think about how to break apart logic all properties you need on objects 

  },

  addClickTracker: function () {

  },

  clickHandler: function (event//takes event in) // { // takes the event into 

  },
};

(function createProducts() {
  ify that takes in all images and runs on page load, takes images and runs
})()